import { noDifferentName } from "./rules/no-different-name";
import { notUsePushInMapRule } from "./rules/notUsePushInMap";

export const rules = {
  "no-different-name": noDifferentName,
  "not-use-push-in-map": notUsePushInMapRule
};